[pytest]
testpaths = tests
# El backend es plano y los módulos se importan por nombre (import pipeline,
# import asignacion). Sin esto, pytest corre desde tests/ y no los encuentra.
pythonpath = .
asyncio_mode = auto
# Silencia el warning de pytest-asyncio sobre el scope por defecto del loop.
asyncio_default_fixture_loop_scope = function
